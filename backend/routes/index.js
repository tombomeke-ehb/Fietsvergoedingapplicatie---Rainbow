// backend/routes/index.js
const express = require('express');
const router = express.Router();

// Hier komen de route imports, bijv:
// router.use('/users', require('./userRoutes'));

module.exports = router;
